// components/landlordVerification/steps/Step4Review.jsx
import { DOCUMENT_TYPES } from "@/constant/docTypes";
import { FiShield } from "react-icons/fi";

export default function Step4Review({
                                        firstName, lastName,
                                        dateOfBirth, address,
                                        citizenship, occupation,
                                        selectedDocument,
                                        uploadedFile, capturedDocument,
                                        selfie
                                    }) {
    return (
        <div className="space-y-6">
            {/* same UI from your Step 4 section */}
        </div>
    );
}
